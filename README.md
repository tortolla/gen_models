# README

Примеры (все запускаются одинаково):
- `models/BLIP`
- `models/BLIP2`
-  .....

Во всех случаях шаги запуска одинаковые 
---

## 2. Структура папки модели

Каждая модель имеет одинаковую структуру:

- `data/`          – входные данные и примеры для тестового инференса (картинки, тексты и т.п.).
- `src/`           – код загрузки модели
- `inference.ipynb` – ,,игрушечный,, пример использования модели
- `requirements.txt` – список зависимостей
- `Dockerfile`     – Docker-образ
- `docker-compose.yml` – сценарий запуска Docker-образа

---

## 3. Требования

1. Docker  
2. Docker Compose  
   - в новых версиях используется команда `docker compose`
   - в старых – `docker-compose`

---

## 4. Как запустить модель

```bash
cd models/<MODEL_NAME>
docker compose up
на портах 127.0.0.1:8888/127.0.0.1:8000 должен открыться ноутбук. (первые две ячейки скачивают модель, далее идет простой пример использования)

